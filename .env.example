# Root environment variables example
# Copy this file to .env and fill in your values

# Your machine's LAN IP address (for Docker network access from other devices)
# Find this with: ipconfig (Windows) or ifconfig (Linux/Mac)
# Examples: 192.168.1.100, 10.0.0.50
HOST_IP=192.168.8.42
